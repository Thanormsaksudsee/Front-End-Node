<!DOCTYPE html>
<html lang="en">
<head>
    <title>Treatment</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #1a1a1a;
            
            margin: 0;
            padding: 0;
            color: #d8f0f3;
            background-size: cover;
        }

        h1 {
            text-align: center;
            color: #2196f3;
        }

        table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 20px;
        }

        th, td {
            border: 1px solid #333;
            padding: 12px;
            text-align: left;
            white-space: pre-wrap;
            font-size: 22px;
        }

        th {
            background-color: #444;
        }

        img {
            max-width: 700px;
            max-height: auto;
            height: auto;
            display: block;
            margin: 0 auto;
            border-radius: 5px;
            cursor: pointer; 
        }

        .actions {
            display: flex;
            gap: 10px;
            justify-content: center;
            margin-top: 20px;
        }

        .link-center {
            text-align: center;
            margin-top: 20px;
        }

        .link-center a {
            display: inline-block;
            margin: 0 10px;
            padding: 10px 20px;
            text-decoration: none;
            color: #fff;
            background-color: #2196f3;
            border-radius: 5px;
            transition: background-color 0.3s ease;
        }

        .link-center a:hover {
            background-color: #1769aa;
        }

        /* Lightbox styling */
        .lightbox {
            display: none;
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0, 0, 0, 0.9);
            justify-content: center;
            align-items: center;
        }

        .lightbox img {
            max-width: 80%;
            max-height: 80%;
            border-radius: 5px;
        }
    </style>
</head>
<body>
    <h1>View Treatment</h1>
    <table class="Treatment-table">
    <tr>
        <th>ID</th>
        <th>DoctorID</th>
        <th>PatientID</th>
        <th>HospitalID</th>
        <th>Treatment</th>
    </tr>
    <tr>
        <td><%= Treatment.ID %></td>
        <td><%= Treatment.DoctorID %></td>
        <td><%= Treatment.PatientID %></td>
        <td><%= Treatment.DoctorID %></td>
        <td><%= Treatment.Treatment %></td>
    </tr>
</table>
<div class="link-center">
    <a href="/updateTreatment/<%= Treatment.ID %>">Edit</a>
    <a href="/updateTreatment/<%= Treatment.ID %>">Delete</a>
    <a href="/">Back to List</a>
</div>
</body>
</html>